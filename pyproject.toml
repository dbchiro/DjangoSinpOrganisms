[tool.poetry]
name = "dj_sinp_organisms"
version = "0.1.0"
description = ""
authors = ["hypsug0 <fcloitre@dbchiro.org>"]
packages = [
    { include = "sinp_organisms" },
]

[tool.poetry.dependencies]
python = ">=3.6.2,<4.0"
Django = "^3.2.5"
django-rest-framework = "^0.1.0"
dj-sinp-nomenclatures = {git = "https://github.com/dbchiro/DjangoSinpNomenclature.git", rev = "dev"}

[tool.poetry.dev-dependencies]
pytest = "^5.2"
black = {version = "^21.6b0", allow-prereleases = true}
Sphinx = "^4.1.1"
sphinx-rtd-theme = "^0.5.2"
rstcheck = "^3.3.1"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
